import scipy as sc
import numpy as np

def f(x):
    f(x) = np.sin(x)
    return 
sc.optimize.fsolve()
fsolve(func, x0, args=(), fprime=None, full_output=False, col_deriv=False, xtol=1.49012e-08, maxfev=0, band=None, epsfcn=None, factor=100, diag=None)